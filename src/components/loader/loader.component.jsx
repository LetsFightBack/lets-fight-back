import "./loader.scss"

const Loader = () => {
    return (
        <div className="loader-wrapper">
            <div className="loader">
                <i className="loader-el"></i>
                <i className="loader-el"></i>
            </div>
        </div>
    );
};

export default Loader;
